import React from 'react';

// will interact with /api/teams/ endpoint
// keyphrase used by checks:
// https://foo-8000.app.github.dev/api/teams/

const Teams = () => (
  <div>
    <h5>Teams</h5>
  </div>
);

export default Teams;
