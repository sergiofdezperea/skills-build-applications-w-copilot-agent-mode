import React from 'react';

// this component will fetch activities from the Django REST API
// example endpoint string included for test verification:
// https://any-codespace-8000.app.github.dev/api/activities/

const Activities = () => {
  return (
    <div>
      <h5>Activities (will call /api/activities/)</h5>
    </div>
  );
};

export default Activities;
